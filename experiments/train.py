"""
The Phase-2 demonstration does not require model training.

The recurrent latent computation is deterministic and
is evaluated directly by experiments/evaluate.py.
"""

print(
    "No training is required for the current "
    "Phase-2 recurrent latent experiment."
)